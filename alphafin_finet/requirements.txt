# AlphaFin 한국판 — 학습 코드 샘플 의존성
# 설치:
#   1) PyTorch는 CUDA 버전에 맞춰 먼저 설치
#      pip install torch --index-url https://download.pytorch.org/whl/cu121
#   2) 나머지
#      pip install -r requirements.txt

# Unsloth (학습 가속 + QLoRA) — transformers/trl 보다 먼저 import 되어야 함
unsloth

# Hugging Face 스택
transformers>=4.51.0
trl>=0.15.0
peft>=0.14.0
accelerate>=0.34.0
bitsandbytes>=0.45.0
datasets>=3.0.0

# 토크나이저/유틸
sentencepiece
protobuf
tqdm

# PyTorch는 CUDA 버전에 맞춰 별도 설치 (위 주석 참조)
# torch>=2.4.0
